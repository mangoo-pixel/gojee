/** @type {import('next').NextConfig} */
const nextConfig = {
  // Turbopack disabled: use `next dev` only (no --turbo) to avoid chunk 404s
  // turbo: { ... } would go here if we re-enabled it
};

module.exports = nextConfig;
